export enum tagTypes {
  category = "category",
  brand = "brand",
  product = "product",
  cart = "cart",
  order = "order",
  failedPayments= "failedPayments"
}
export const tagTypeList = [tagTypes.category, tagTypes.brand, tagTypes.product, tagTypes.cart, tagTypes.order, tagTypes.failedPayments]
